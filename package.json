{
  "name": "ca3-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "CONTROL-DE-ASISTENCIA",
    "CONTROL-DE-ASISTENCIA-API"
  ],
  "scripts": {
    "deploy:local": "node scripts/increment-version.js && npm run build:all && scripts\\deploy.bat",
    "deploy:local2": "npm run build:all && scripts\\deploy_CA.bat",
    "build:api": "tsc -p CONTROL-DE-ASISTENCIA-API/tsconfig.json",
    "build": "npm run build --workspace=CONTROL-DE-ASISTENCIA",
    "export:sql": "node scripts/export-sql.js",
    "deploy:installer": "node scripts/export-sql.js && node scripts/extract-seeds.js && node scripts/increment-version.js && npm run build:all && node scripts/packager.js",
    "deploy:installer:exe": "node scripts/export-sql.js && node scripts/extract-seeds.js && node scripts/increment-version.js && npm run build:all && node scripts/packager.js --exe",
    "deploy:prod": "echo 'Despliegue a producción (Render) se maneja automáticamente al hacer push a la rama main.'",
    "build:all": "npm run build --workspaces"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "archiver": "^7.0.1",
    "dotenv-cli": "^10.0.0",
    "mssql": "^10.0.2"
  },
  "dependencies": {
    "@tanstack/react-virtual": "^3.13.12",
    "date-fns": "^4.1.0",
    "jspdf": "^4.2.1",
    "jspdf-autotable": "^5.0.2",
    "node-cron": "^4.2.1",
    "xlsx": "^0.18.5"
  }
}
